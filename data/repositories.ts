import { Repository } from "@/types";

// Static fallback list. Replace or extend with real repository details.
// If you'd rather pull this dynamically from the GitHub API at build time,
// see lib/github.ts for a minimal fetch helper you can wire into this file.
export const repositories: Repository[] = [];
